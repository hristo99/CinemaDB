extends layout

block content
	script(type='text/javascript').
		function getMovieData(id) {
			$(location).attr('href','/movie/' + id);
		};
	p Movies
		ul
			each m in movies
				li(onclick="getMovieData(" + m.Id + ")") #{m.Title}
				li 
